主題：小費計算器 — 精簡學習記錄

重點：
- `prompt()` 取得使用者輸入，`alert()` 回饋，`console.log()` 偵錯。
- `parseInt()` 將輸入轉為數字，計算小費（20%）與總額。

問題：
- `parseInt()` 會捨去小數；若輸入非數字會得到 `NaN`。

改進建議：
- 改用 `parseFloat()` 支援小數，並用 `toFixed(2)` 格式化；加入輸入驗證或改為表單輸入以改善 UX。